import React from 'react';

const PageLoader = () => {
    return  (
        <div className="page-loader">
            <div className="spinner"></div>
        </div>
    );
};

export default PageLoader;
